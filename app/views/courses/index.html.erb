<br />

<div class="booyah-box col-10 offset-1">
    <h1 class="text-center">Choose your own adventure.✨</h1>
    <hr />
    <br />
    <% @courses.each do |course| %>
    <h2 class="course-title text-left">
    <%= link_to course.title, course_path(course) %>
    </h2>
  <br />
  <div class="row">
    <div class="course-description col-sm-8 col-12">
      <%= course.description %>
    </div>
    <div class="col-sm-4 col-12">
      <%= link_to 'Details', course_path(course), class: 'details-btn btn btn-primary btn-large offset-3' %>
    </div>
  </div>
  <br class="clr" />
<% end %>
</div>

